把代码先彻底抛开，我们站在系统架构师的视角，把整个后端的数据流、逻辑判断和模块分工像搭积木一样理顺。

这个后端本质上只做三件事：听懂需求（Agent决策）、脏活累活（爬虫与清洗）、打包交付（流式输出）。

一、 核心执行路线图（五步极简心智模型）
把整个请求生命周期拆解为清晰的 5 个阶段：

[前端用户提问]
       │
       ▼
【第 1 站：意图门禁】 ──(纯闲聊/常规问答)──► [直接调大模型生成文本] ──► 结束推流
       │
   (需要找资源)
       ▼
【第 2 站：参数提取】 ──► 大模型提取标准关键词（例如："攻壳机动队 1080P"）
       │
       ▼
【第 3 站：工具执行】 ──► 触发爬虫流水线
       │                   ├─ 优先查缓存（Redis 是否爬过？）
       │                   ├─ 极速抓取（Jsoup 请求静态 HTML）
       │                   └─ 兜底抓取（Jsoup 失败则切 Playwright 渲染 JS）
       │
       ▼
【第 4 站：漏斗清洗】 ──► 剔除多余标签与脚本，只留「标题+播放链接+简介」组装成极简卡片
       │
       ▼
【第 5 站：结果聚合与推流】 ──► 大模型结合抓取结果给出评价，后端通过 SSE 像打字机一样逐字推给前端
二、 关键节点的设计原则（少走弯路的黄金法则）
1. 爬虫层：双模降级与资源保护
别把大模型当万能工具：大模型只负责决定何时去爬、搜什么词，绝对不要让它直接去读原生网页源码。

分级试探策略：

第一梯队（Jsoup）：像轻骑兵，发个纯 HTTP 请求，200 毫秒内搞定，省内存、省 CPU。

第二梯队（Playwright）：像重装装甲，只有当 Jsoup 拿回来的页面全是空白或只带 JS 脚本标签时，才召唤无头浏览器去跑渲染。

并发限流（防炸内存）：Playwright 背后是真实的浏览器内核，必须设一道“闸门”（比如最多允许 3 个并发网页渲染），多出来的请求要么排队，要么降级提示“系统繁忙”。

2. 数据清洗层：信息漏斗（必须在喂给大模型前完成）
体积截断：一个网页可能有几万行 HTML，如果全部送进大模型，不仅调用费用暴增，还会导致大模型“注意力分散”（幻觉变严重）。

严格只提取三要素：抓取器内部必须把 HTML 榨干成纯粹的结构化数据，只保留：

资源标题

真实超链接

简短描述/画质

数量截断：无论页面搜出来多少条，只取前 3~5 条最相关的给大模型做总结。

3. 交互层：打字机推流与用户体验
避免白屏假死：爬虫 + 大模型整体通常需要耗费 3~6 秒。如果不做流式输出，用户界面会长时间处于转圈无响应状态，极易被判定为挂掉。

状态分段通知：

阶段 A：建立连接，先推送状态提示（如：“正在理解您的意图...” -> “正在全网嗅探相关资源...”）。

阶段 B：拿到爬取结果，大模型开始润色，逐字推回观影指南与推荐指数。

三、 推荐你的学习与研发节奏（步步为营四步走）
作为一个初学者，千万不要试图一天之内把所有组件同时接通，那样一旦报错完全不知道是哪一层出的问题。

第 1 步：单测纯爬虫
写一个最简单的 Java 主函数（main 方法），不接任何大模型，只练习用 Jsoup 抓取某个目标网站的搜索结果，能把标题和链接用 System.out.println 打出来就算过关。

第 2 步：跑通纯 Agent 工具调用
搭建一个极简的 Spring Boot，定义一个写死假数据的本地方法（比如返回两行假链接），测试 LangChain4j 的大模型在遇到“帮我找动漫”时能不能准确自动调用这个本地方法。

第 3 步：模块合体（工具接入真爬虫）
把第 1 步调通的真实爬取代码，塞进第 2 步的工具方法里，观察大模型能不能吃下爬出来的真实数据并总结。

第 4 步：接入流式接口与前端对接
最后再把控制器的返回值改成流式事件，配合前端的 shadcn/ui 气泡框，实现最终的打字机特效。
